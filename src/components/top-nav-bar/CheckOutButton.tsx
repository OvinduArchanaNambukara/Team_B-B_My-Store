import React from "react";
import {Button} from "react-bootstrap";

const CheckOutButton: React.FC = () => {
    return (
        <Button className='mr-1 mr-sm-4 d-none d-sm-block'>Check Out</Button>
    );
}

export default CheckOutButton;
